# Development mode — runs tsx with source files directly
FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

COPY tsconfig.json ./
COPY src/ ./src/

ENV NODE_ENV=development

CMD ["npx", "tsx", "src/index.ts"]
